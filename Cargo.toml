[workspace]
resolver = "2"
members = [
    "command-center",
    "stt",
    "stt-api",
    "llm",
    "llm-api",
    "tg",
    "tg-api",
]

[profile.release]
panic = "abort"
opt-level = "s"
lto = true
